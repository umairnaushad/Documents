1)  docker image ls
2)  docker image history container_name
3)  docker image inspect container_name
4)  docker image tag nginx:latest umairnaushad/nginx

5)  docker image build -t imageName .
    docker image build -t imageName -f DockerfileName

6)  docker image prune        (to remove up dangling images)
    docker image prune -a     (to remove all images not using)
7)  docker system prune       (to remove up everything)
8)  docker system df          (to see space usage)


20) docker-compose up
    docker-compose up -d
    docker-compose up --build
21) docker-conpose down
    docker-compose down --rmi local
    
31) docker swarn init --advertise-adr 192.168.1.x
32) docker node ls

41) docker service create alpine ping 8.8.8.8
42) docker service ls
43) docker service ps <name>
44) docker service update <ID> --replicas 3



51) docker-machine create node1     (virtual box should be installed for this)
52) docker-machine ssh node1
53) docker-machine env node1
